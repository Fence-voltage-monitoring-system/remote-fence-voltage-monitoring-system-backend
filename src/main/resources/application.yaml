spring:
  application:
    name: elephant-fence-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/elephant_fence}
    username: ${DB_USERNAME:thamindubandara}
    password: ${DB_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    secret: ${JWT_SECRET:RemoteElephantFenceMonitoringSystemSecretKey2026SecureKey123!}
    expiration-ms: 900000 # 15 minutes
    refresh-expiration-ms: 604800000 # 7 days
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:8080}


